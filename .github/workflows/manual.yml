name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        default: "Hello"
        type: string
        required: true
        description: A cheerful word
jobs:
  manual:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.greeting }}"
